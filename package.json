{
	"devDependencies": {
		"typescript": "^4.4.3"
	},
	"dependencies": {
		"axios": "^0.21.4",
		"concurrently": "^7.4.0",
		"json-server": "^0.17.0"
	},
	"scripts": {
		"start:db": "json-server -w db.json",
		"start:parcel": "parcel index.html",
		"start": "concurrently npm:start:*"
	}
}
